export const metadata = {
  title: "Create Account | KAGAMI",
  description: "Create a KAGAMI account for faster checkout.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function RegisterLayout({ children }) {
  return children;
}
