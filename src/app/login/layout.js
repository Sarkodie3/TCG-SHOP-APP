export const metadata = {
  title: "Login | KAGAMI",
  description: "Login to your KAGAMI account.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function LoginLayout({ children }) {
  return children;
}
