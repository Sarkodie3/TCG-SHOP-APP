export const metadata = {
  title: "Order Submitted | KAGAMI",
  description: "KAGAMI checkout confirmation.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function CheckoutSuccessLayout({ children }) {
  return children;
}
